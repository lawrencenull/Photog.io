<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');
/*
if ( ! function_exists('follow_toggle_button')) {
    function follow_toggle_button() {

        $ci =& get_instance();
        $ci->load->model('');

        if ( false ) {
            $view = 'subscription/follow_button';
        } else {
            $view = 'subscription/unfollow_button';
        }

        
        $ci->load->view( $view );

    }
}*/